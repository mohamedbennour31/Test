<!DOCTYPE html>
<html>
	<head>
		<title>Détails de l'Auteur</title>
	</head>
	<body>
		<h1>Détails de l'Auteur</h1>

		<p>ID:
			{{ author.id }}</p>
      			<p><img src="{{ author.picture }}" alt="{{ author.username }}" width="100"/></p>
		<p>Nom d'utilisateur:
			{{ author.username }}</p>
		<p>Email:
			{{ author.email }}</p>
		<p>Nombre de livres:
			{{ author.nb_books }}</p>

		<a href="{{ path('app_listAuthors') }}">Retour à la liste des auteurs</a>
	</body>
</html>
